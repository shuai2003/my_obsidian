cpu能读懂的最小单位-比特位 bit      b
内存机构的最小寻址单位-字节-Byte  B
1B = 8b
0-255  n比特位1=2的n次方减1  32个比特位1=2的32次方-1
![[Pasted image 20260522154036.png]]
![[Pasted image 20260522154618.png]]

int 第一位是符号位  int有符号的占位符%d  unsigned int无符号的占位符是%u
![[Pasted image 20260522160944.png]]
有符号的整数 int  左边第一位是符号位 
![[Pasted image 20260522161403.png]]![[Pasted image 20260522161549.png]]![[Pasted image 20260522161605.png]]
正数多一个特殊值0  所以负数到-128 
![[Pasted image 20260522163017.png]]![[Pasted image 20260522163036.png]]
## 字符和字符串（c语言没有字符串类型）用char类型定义字符串
**字符串就是指向字符的指针**
C 语言用 **字符数组 + 结束符 `\0`** 来模拟字符串
1.字符串常量一旦确定下来就不能改变了 
2.字符类型的数组来存放和处理字符串
char str1[] = {'f','i','s','h','C'}; 
char str1[] = {"fishC"};用字符串常量初始化字符数组 ，可以省略大括号
char str1[] = "fishC";
可以不写元素的个数，因为编译器会自动计算
字符变量可以用来存放整数？ 字符类型是一个特殊的整型
二进制和英语字符一一对应  往一个变量里面存放67这个数字，定义为字符变量，输出时指定%c ,就会到这个表格查对应的字符 c
![[Pasted image 20260522164905.png]]![[Pasted image 20260522170348.png]]![[Pasted image 20260522170455.png]]
c语言如何处理字符串常量的：为了确定字符串在内存里面的结束位置 :在字符串最后加上一个反斜杠0这个空字符来表示结束
![[Pasted image 20260522172752.png]]
字符串常量 你不用在末尾加上一个反斜杠0，它自动会帮你添加
![[Pasted image 20260522173032.png]]
![[Pasted image 20260522173143.png]]
### 字符串处理函数
如果能用得上官方提供的现成的函数的话，尽量不要自己去写，**避免重复造轮子**
![[Pasted image 20260523171846.png]]
![[Pasted image 20260523174316.png]]
## 运算符   有1.优先级和2.方向性的
![[Pasted image 20260522173341.png]]
最常用的运算符 ：算术运算符
![[Pasted image 20260522173918.png]]
![[Pasted image 20260528210255.png]]
## 指针   指针的类型就是它指向元素的类型
 指针的原理-->数据在内存中是如何读取的 
 在程序中定义一个变量   在程序进行编译时，系统就会根据变量的类型在系统中分配对应的长度空间，我们如何来访问这个变量   通过变量名访问内存是一种比较安全的方式    
 内存的最小索引是一个字节 每个地址可以存放一个字节的数据  存放一个int变量  那么总共需要用到4个存储单元     int  4个字节
 可以把内存想象为一个超大的字符数组 
 在内存中完全没有必要存放变量名的   变量名是为了程序员的使用而定义的
 当你读取某个变量的时候 编译器就会通过变量名所在的地址  并根据变量的类型 读取相应范围的数据
![[Pasted image 20260524173713.png]]
通常的指针指的是**地址**   c语言有专门的指针变量用来存放指针  跟普通变量又不同   指针变量存放的是一个地址  普通变量存放的是数据    
**指针变量也有类型 它的类型就是存放的地址指向的数据类型**
地址-->称为指针
**存放地址的变量-->指针变量**
用指针变量pa存放变量a的地址
**定义指针的类型** 是指针变量中存放的地址指向的内存单元的数据类型
![[Pasted image 20260524174239.png]]![[Pasted image 20260524174302.png]]
**pa是a的地址**
这里注意取值运算符和定义指针用的型号是同一个符号（符号的重用）
直接访问：直接通过变量来访问变量的值
间接访问：通过指针变量(指针中存放的地址)通过这个地址间接的访问
野指针
![[Pasted image 20260524180308.png]]

## 指针和数组   数组名就是指针
数组不就是指针吗？不是
![[Pasted image 20260524203452.png|697]]在scanf("%d",&n);  &是取值操作符  如果存储的这个位置是指针变量 不需要&
![[Pasted image 20260524214304.png]]![[Pasted image 20260524214700.png]]
语句1和语句2是等价的

![[Pasted image 20260524215823.png]]
   ![[Pasted image 20260524215933.png]]
   **定义指针指向字符串**
   ![[Pasted image 20260524220157.png]]
![[Pasted image 20260526202347.png]]
## 指针数组、数组指针
指针和数组在很多方面都可以替换
**指针变量和字符串这个名都是指向地址，但是指针变量他是一个左值，字符串名是一个地址常量（它不说左值）  字符数组只是地址不是左值

![[Pasted image 20260526203404.png]]
### 指针数组、数组指针
指针数组是数组；   数组指针是指针
![[Pasted image 20260528205827.png]]
![[Pasted image 20260530221556.png]]
![[Pasted image 20260530221617.png]]
分析指针数组可以从运算符的优先级和结合性入手
数组下标[]的优先级高于取值运算符 *     所以p1先和5结合  p1被定义为5个元素的数组       数组元素类型是int  *  -是指向整型变量的指针
### 指针数组
字符串在c语言中，有一个特殊的约定，你只需要指向这个字符串的起始地址，然后它一个一个字节的读下去，直到读到'\0'为止 表示字符串结束
![[Pasted image 20260528210843.png]]![[Pasted image 20260528210906.png]]
指针数组如何初始化：
![[Pasted image 20260528211003.png]]
它在指向字符指针的时候就非常好用
![[Pasted image 20260528215729.png]]

### 字符串和指针的关系
%s输出的是字符串......这里每个P1[ ]相当于一个一维数组。所以他们的名字代表首地址，不需要&
每个字符串相当于 这样的指针用法  char * p = “sss”;  p指向数组名

字符串本质上是一段连续的内存数据，而指针是用来存放这段数据“起始地址”的变量（路标）。

![[Pasted image 20260528214404.png|697]]
![[Pasted image 20260528215759.png]]
### 数组指针   p2这个指针指向的数组的类型是int  有5个元素
![[Pasted image 20260528215851.png]]
圆括号和数组下标在同一个优先级队列里面的  优先级是一样的看它的结合顺序（从左到右）
p2先被定义为指针变量    指向5个元素的类型
数组指针就是指向数组的指针
![[Pasted image 20260528221420.png]]![[Pasted image 20260528221427.png]]![[Pasted image 20260528221528.png]]
代码有问题  p2是个指针   指针必须给它一个地址才行    否则就是野指针

#### 指向数组首地址的指针 --指向数组的指针
1.![[Pasted image 20260528222203.png]]
这个案例的  指针指向数组的第一个元素的地址 事实上是一个指向整型变量的指针    它指向的是数组的第一个元素   而不是指向数组  因为数组的第一个地址也是等于=数组名 =等于数组首地址  数组元素是挨个存放的
p指向的是一个变量   不是指向数组

##### 指针数组才是---指向数组的指针
##### 整个数组的地址等于==数组首元素的地址  就是下面temp = &temp

**p2 里面存的是整个数组的地址**
p2 指向一个包含5个int元素的数组
p2指向整个数组的地址
&temp  把数组当成一个整体   取出数组的地址给指针变量
这里等于temp，相当于等于&temp[0]，但是数组指针指向的是整个数组，所以&temp才是对的，&"temp"这整个数组
![[Pasted image 20260528223937.png]]
- `temp` ：数组首元素地址
- `&temp` ：整个数组地址
- **`&arr[0]`（对首元素取地址）**：
- **`&arr`（对整个数组取地址）**：
![[Pasted image 20260528225842.png]]
这里的p2才是整个数组的地址，因为上面赋值赋的是&temp，&temp代表的就是整个数组的地址。* p2是数组首元素地址，* （* p2+i）就能对数组里的值进行遍历了
p2对应&temp整个数组的地址，* p2对应&temp的值，也就是temp这个数组名，数组名＝首元素地址，剩下的应该理解了把

```
printf("p2 = %d\n",*(*p2++));的错误使用 
```
![[Pasted image 20260529113155.png]]
### 二维数组和指针
c语言并没有真正意义上的二维数组
二维数组的实现 是通过线性扩展实现
![[Pasted image 20260529220726.png|697]]
array 表示什么 ？
array是二维数组的首地址
array指向包含5个函数的指针
```（**array指向第1个元素array[0][0]）
```
![[Pasted image 20260529220931.png]]![[Pasted image 20260529223821.png|669]]
array是指向5个元素的指针
array+1 也是指向5个元素的指针
对array+1解引用
![[Pasted image 20260529224343.png]]
```
*(array+1) 是array+1的解引用
将取值运算符作用在地址上，就是把一个地址的值取出来 --称为解引用
```
![[Pasted image 20260529224413.png]]
```array + 1是指向向5个元素的指针
```array[1]是array[1][0]数组的名字
然后对array+1解引用是得到a[1]  a[1]是一维数组的名字
对*(array+1)解引用是首地址的值
*是取值符，获取里面的值，不加*只是表示获取第二行的地址
语法 
```
![[Pasted image 20260530170955.png]]
![[Pasted image 20260530172800.png]]**在c语言中所谓的多维数组，事实上都是一维数组的线性扩展得到的，它事实上也是线性的方式存储的
我们可以用这种间接引用的方式来实现
下标索引的形式都可以转化为使用指针简介索引的形式
```
*(array+i)==array[i]是完全等价的
```
指针和数组的亲密关系**
![[Pasted image 20260530174711.png]]
![[Pasted image 20260530174811.png]]
![[Pasted image 20260530222007.png]]
### void指针
void  ---  无类型的意思  不应该用它来定义一个变量
定义变量的啥时候，我们通过类型来决定变量所占坑位大小(变量所占的内存空间的大小)   char -- 只占一个字节空间  int 占四个字节
![[Pasted image 20260531135354.png]]
**如果将void类型指针转化为其他类型的指针，那我们就需要加上一个强制类型转换
把void类型强制转化成整形  你的void类型里面指向的数据是整型
不要直接对void指针进行解引用  编译器搞不懂你指向的数据宽度 void是无类型
![[Pasted image 20260531150636.png]]字符串只需要指针指向地址就可以打印出来
void指针可以间接的它可以支持，不同类型的指针之间的合法转换
#### NULL指针 空指针   对NULL指针解引用是非法的
一个指针不指向任何数据，称为空指针
**`(void *)0`**：把整数 `0` 强制转换成一个“无类型指针”。
![[Pasted image 20260531153321.png]]![[Pasted image 20260531153340.png]]
![[Pasted image 20260531154128.png]]
NUL = '\0'
###  指向指针的指针
![[Pasted image 20260531155452.png]]

![[Pasted image 20260531161919.png]]**指向指针的指针的应用**
故意把简单的复杂化。就是指针数组取地址给二级指针数组赋值。正常写代码很少这么用.一直绕来绕去
![[Pasted image 20260531174101.png]]
![[Pasted image 20260531173947.png]]
![[Pasted image 20260531174358.png]]

![[Pasted image 20260531213145.png]]
**利用跨度是一样的**
![[Pasted image 20260531215817.png|619]]
![[Pasted image 20260531215258.png|559]]
p和array 的跨度一样 才能赋值 p=array
如何用指针指向二维数组
跨度问题？
![[Pasted image 20260531220002.png|605]]
![[Pasted image 20260531215941.png|591]]
### 常量和指针  1.指向常量的指针 2.常量指针
![[Pasted image 20260531220420.png|566]]
const  失去了 写入和修改的能力，只剩下只读的功能
**指向常量的指针**  
万能的指针 void * ，当然也可以指向被const修饰过的变量
因此我们不能通过指针修改它所引用的值
![[Pasted image 20260531222259.png|519]]
![[Pasted image 20260531222416.png|487]]
**2.常量指针**
![[Pasted image 20260602214459.png]]
把const放在* 左边 是指向常量的指针
把const放在* 的右边 是常量指针
常量指针  指针变量本身不能改变 p=&cnum; x
指向的值可以被改变 * p =1024 √
![[Pasted image 20260602215023.png|629]]
指向非常量的常量指针
![[Pasted image 20260602215117.png]]
指向常量的常量指针
![[Pasted image 20260602215149.png]]
![[Pasted image 20260602215451.png|577]]
### 函数
![[Pasted image 20260602215553.png|535]]![[Pasted image 20260602220223.png|532]]
类型名就函数的返回值
void  不返回任何数据  不写默认返回整型
![[Pasted image 20260602220547.png|543]]![[Pasted image 20260602221522.png|545]] 
通常函数会通过返回值来说明函数是否调用成功
main函数返回0 表示调用成功
函数的参数的数量是可变的
函数定义的时候写的参数就叫**形参**   **只有在函数内部有效**
![[Pasted image 20260605165341.png]]
函数被调用的时候，传给它一个实际的值  **实参**
![[Pasted image 20260605165331.png]]
形参和实参的功能就是用于数据传输
当函数调用时，实参的值会传递给形参(单向性)
![[Pasted image 20260602222423.png]]
指针也是变量 可以通过参数传递给函数
定义函数时  就已经确定了参数的数量和类型


**函数   指针变量参数**
![[Pasted image 20260605171254.png|402]]
每个函数都有独立的作用域（每个函数的内部都是独立的）
他们的变量只在函数内部生效
不同函数之间是不能直接访问对方的变量
![[Pasted image 20260605171656.png|430]]
指针存放的是别人的地址
传递的地址 可以是不同的函数之间 间接访问对方的变量

#### **传数组**
![[Pasted image 20260605174448.png|421]]
并不存在将整个数组传递作为参数传递的这么一个方式
**它只是将数组的首地址传过去了**
别忘了之前学过的，数组名就是第一个元素的地址，也就是指针，所以传过去的还是指针
传过去的数组要在函数中用新的数组接收吗，防止数据被修改

![[Pasted image 20260605174016.png]]
![[Pasted image 20260605174034.png]]

**printf()的参数变量是可变的**
#### 可变参数
![[Pasted image 20260605185723.png|501]]![[Pasted image 20260605192827.png]]
### 指针函数和函数指针  函数指针和函数有相应的关系
 
 
##### 指针函数
 函数的类型 指的函数的类型（int、flloat、char、**指针类型**）
 #### **用指针变量做函数的返回值 称为指针函数**
 函数返回指针类型的数据：
 int * get_name() 
 ![[Pasted image 20260605200234.png|617]]
用char类型指针定义字符串
char类型的指针是指向一个字符，我们用它来指向字符串的第一个字符
字符串约定俗成是截止于空字符（'\0'）,所以我们知道它第一个字符，我们就知道整个字符串
**定义char类型的指针，我们可以接收一个返回的字符串**，返回的是A的地址
##### 不要返回局部变量的变量的指针
在函数里面定义的变量是局部变量 ->不要在函数内返回局部变量的地址
局部变量的有效范围（作用域）：仅限于函数的内部
![[Pasted image 20260605205030.png|581]]
这个apple的字符串只存在于这个getWord函数内部，你把它的地址返回给main() 当我们的程序执行到这里，获得你的地址的时候，它这个apple字符数组已经销毁了
字符串没有定义到变量里面去，字符串在c语言中，它会去找到一个固定的存储区域，一个常量存储区域来存放，因为你发现字符串是不可变的，它的存储位置不在函数里面
 
#### 函数指针是指针  是指向函数的指针
**函数名相当于这个函数的地址**
![[Pasted image 20260605221441.png|589]]
![[Pasted image 20260606153041.png]]



#### 函数指针做参数传递的时候和做返回值的时候，应该怎么应用它
1.函数指针作为函数的参数    函数的参数
然后我再来声明一个函数，这个函数的参数是一个函数指针，也就是说，这个函数的参数的其中一个参数是一个函数的地址
声明一个函数 //这个函数的参数是一个函数指针也就是说这个函数的参数其中的一个参数是一个函数的地址
```
int (*fp)(int,int);//定义函数指针
fp = add;  //fp指向函数的地址  函数名相当于函数的地址
int calc(int (*fp)(int,int),int num1,int num2);//第一个参数是函数指针做参数  函数定义指针 --传入的是指针指向的 int *p;定义指针 p=&a;//指针赋值
int result1 = calc(add,num1,num2);//传入函数定义的指针传入应该是地址 函数名相当于函数的地址 
```
![[Pasted image 20260606164210.png|457]]
##### 2.函数指针作为函数值 返回函数指针 定义一个函数指针来接收

**错误写法--编译器会以为你在一个语句里面，写了两个函数的定义**
![[Pasted image 20260606164611.png|523]]

**正确写法**
![[Pasted image 20260606165430.png|513]]
![[Pasted image 20260606202710.png|627]]
![[Pasted image 20260606203234.png]]
select函数返回值，（就是一个返回整形并且带有两个参数的函数指针）
![[Pasted image 20260606203346.png]]

### 局部变量-----全局变量
#### 局部变量
作用范围
学习了函数才发现 不同函数之间定义的变量原来是无法访问的
![[Pasted image 20260606215049.png|540]]
![[Pasted image 20260606215447.png]]
i的作用范围仅限于{}之间
#### 全局变量

![[Pasted image 20260606221851.png|548]]
![[Pasted image 20260606221756.png|523]]![[Pasted image 20260606221939.png|524]]
extern 事实上可以用于多个源文件之间，使用同一个变量  多文件编程ddddd
![[Pasted image 20260606222124.png|581]]
### 作用域
![[Pasted image 20260606222241.png|600]]
1.代码块作用域：位于花括号之间的所有语句
![[Pasted image 20260606222413.png|611]]![[Pasted image 20260606222514.png|605]]![[Pasted image 20260606222813.png|605]]![[Pasted image 20260606222851.png|611]]
![[Pasted image 20260606222935.png|616]]![[Pasted image 20260606223456.png|616]]![[Pasted image 20260606223546.png|617]]![[Pasted image 20260606223627.png|615]]![[Pasted image 20260606223640.png|617]]![[Pasted image 20260606223748.png|620]]
### 生存期
![[Pasted image 20260606224438.png|629]]![[Pasted image 20260606224533.png]]
![[Pasted image 20260606224816.png|569]]![[Pasted image 20260606225005.png|619]]